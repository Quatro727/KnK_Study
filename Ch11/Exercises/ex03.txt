We can correct the function avg_sum like this way:

void avg_sum(double a[], double *avg, double *sum){
    int i;

   *sum = 0.0;
    for(i = 0; i < n; i++){
        *sum += a[i];
    }
    *avg = *sum / n;
}
