At the declaration part of pointer q is incorrect.

In this declaration, since the pointer p hasn't been initialized, we don't know where it's pointing. It could point the memory that prevented by OS. 

So we have to initialize in this way

char *duplicate(char *q, const char *p){
    strcpy(q, p);
    return q;
}

to enable the copying
