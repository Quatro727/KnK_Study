unsigned int reverse_bits(unsigned int n)
{
    int i;
    unsigned int check_bit = 0x01;
    unsigned int result = 0;

    for(i = 0; i < 32; i++ ) {
       unsigned int current = (n >> i) & check_bit;
       result |= (current << (31 - i));
    }

    return result;
}
