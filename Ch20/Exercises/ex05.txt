#define GET_RED(color) ((color) & 0xff)
#define GET_GREEN(color) ((color >> 8) & 0xff)
#define GET_BLUE(color) ((color >> 16) & 0xff

we just want to extract last 8bit(1byte) from color date. 
So we use 0xff rather than 0xffffffff.
