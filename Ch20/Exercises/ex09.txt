(a) 

int count_ones(unsigned char ch) 
{
    int check_bits = 0x01;
    int i;
    int cnt = 0;
    for(i = 0; i < 8; i++) {
        if((ch & check_bits) != 0) 
            cnt++;
        check <<= 1;
    }
    return cnt;
}

(b) 
int count_ones(unsigned char ch)
{   
    int cnt = 0;

    if(ch & 0x01) cnt++;
    if(ch & 0x02) cnt++;
    if(ch & 0x04) cnt++;
    if(ch & 0x08) cnt++;
    if(ch & 0x10) cnt++;
    if(ch & 0x20) cnt++;
    if(ch & 0x40) cnt++;
    if(ch & 0x80) cnt++;
    
    return cnt;
}




