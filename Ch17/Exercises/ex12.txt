struct node {
    int value;
    struct node *next;
};

struct node *find_last(struct node *list, int n){
    struct node *result = NULL;

    while(list != NULL) {
        if(list->value == n) result = list;

        list = list->next;
    }

    return result;
}
